import { SearchfoodPipe } from './searchfood.pipe';

describe('SearchfoodPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfoodPipe();
    expect(pipe).toBeTruthy();
  });
});
